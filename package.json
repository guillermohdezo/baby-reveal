{
  "name": "baby-reveal-app",
  "version": "1.0.0",
  "description": "Interactive Baby Gender Reveal Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && npm run build",
    "build:client": "cd client && npm install && npm run build",
    "render-build": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "cors": "^2.8.5",
    "uuid": "^9.0.0",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "author": "Baby Reveal Team",
  "license": "MIT"
}