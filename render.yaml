services:
  - type: web
    name: baby-reveal-app
    runtime: node
    buildCommand: npm run render-build
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000